package com.tungnk123.musicplayercompose.player

enum class PlayerState {
    STATE_IDLE,
    STATE_READY,
    STATE_BUFFERING,
    STATE_PLAYING,
    STATE_PAUSE,
    STATE_ERROR,
    STATE_END,
    STATE_NEXT_TRACK
}
